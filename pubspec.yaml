name: projectmad
description: A Flutter e-commerce project

publish_to: 'none' # This project is not published to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.7.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.26.0
  firebase_auth: ^4.6.3
  cloud_firestore: ^4.10.0
  firebase_messaging: ^14.7.0

  # State management
  provider: ^6.0.5

  # UI
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
